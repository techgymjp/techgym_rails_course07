<h1><%= Product.model_name.human %>情報 表示</h1>


<table class="table table-striped">
  <tbody>
    <tr>
      <td><%= Product.human_attribute_name(:admin_user_id) %></td>

      <td><%= @product.admin_user_id %></td>
    </tr>
    <tr>
      <td><%= Product.human_attribute_name(:category_id) %></td>

      <td><%= @product.category_id %></td>
    </tr>
    <tr>
      <td><%= Product.human_attribute_name(:title) %></td>

      <td><%= @product.title %></td>

    </tr>
    <tr>
      <td><%= Product.human_attribute_name(:description) %></td>

      <td><%= @product.description %></td>
    </tr>
  </tbody>
</table>

<%= link_to '編集', edit_admin_product_path(@product), class: "btn btn-primary" %>
<%= link_to '戻る', admin_products_path, class: "btn btn-secondary" %>